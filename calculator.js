var calc = {};

calc.add = function(first, second) {
    return first + second;
}


calc.mul = function(first, second) {
    return first * second;
}

calc.divide = function(first, second) {
    return first / second;
}

module.exports = calc